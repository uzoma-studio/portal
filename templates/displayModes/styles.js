import styled from 'styled-components'

export const StyledDisplayModeText = styled.p`
    color: inherit;
    margin-top: 0.25rem;
    font-size: 1.5rem;
    text-align: center;
    overflow: hidden;
    transition: color 0.2s ease;
`